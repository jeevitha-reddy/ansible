# play level variables
- name: variables from play level
  hosts: localhost
  tasks:
  - name: check variables
    ansible.builtin.debug:
      msg: "Hi i am {{Course}},Trainer is {{Trainer}},Duration is {{Duration}}"  


